spring:
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:jenzi}
    password: ${DB_PASSWORD:jenzi@2022}
    url: jdbc:mysql://${DB_HOST}/${DB_NAME:jenzi}