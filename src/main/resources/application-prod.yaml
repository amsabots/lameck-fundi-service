spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
    database: postgresql
  datasource:
    driver-class-name: org.postgresql.Driver
    username: ${DB_USERNAME:jenzi}
    password: ${DB_PASSWORD:jenzi@2022}
    url: jdbc:postgresql://${DB_HOST:localhost}/${DB_NAME:jenzi}